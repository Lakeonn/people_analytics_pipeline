name: 'people_analytics_dbt'
version: '1.0.0'
config-version: 2

profile: 'people_analytics_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  people_analytics_dbt:

    bronze:
      +materialized: view
      +schema: bronze

    silver_current:
      +materialized: table
      +schema: silver

    gold:
      +materialized: table
      +schema: gold